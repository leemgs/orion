# Core (CPU-only, required for simulated reproduction)
numpy>=1.24
scipy>=1.11

# Optional: GPU-accelerated inference backends
# torch>=2.4          # PyTorch with CUDA 12.x
# vllm>=0.5.3         # vLLM serving
# deepspeed>=0.14     # DeepSpeed-Inference
# flexgen             # FlexGen offloading

# Optional: visualisation
matplotlib>=3.8

# Live measurement (--mode live). Install with: pip install -e '.[gpu]'
# Both are hard requirements for the CUDA backend; it refuses to run without
# them rather than falling back to synthetic records.
# torch>=2.4          # CUDA events, streams, pinned host staging
# pynvml>=11.0        # clock gate, power integration, PCIe throughput readback
#
# T_mem additionally needs Nsight Compute (`ncu`) on PATH for HBM counters.
# It ships with the CUDA Toolkit and is not a pip dependency. Without it,
# t_mem is recorded as NaN — never estimated.
# pyamdgpu            # AMD ROCm monitoring (MI250)

# Development
pytest>=7.4
